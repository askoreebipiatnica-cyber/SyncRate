rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Client-side direct access is disabled; all operations are securely handled via server-side Admin SDK
      allow read, write: if false;
    }
  }
}
